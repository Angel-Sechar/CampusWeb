#npm version
#10.8.2

# Node.js version
#20.19.2

#Angular CLI version
#18.0.5

#Maven version
Apache Maven 3.9.18 5f519b97e944483d878815739f519b2eadeea91d

#Java version
java version "21.0.5" 2024-18-15 LTS

#OS WINDOWS 11 WIN32 X64

#Spring boot
3.4.7
###Dependencies

#Database in Docker
docker run -d --name sqlserver-dev -e "ACCEPT_EULA=Y" -e "SA_PASSWORD=*******" -p 1433:1433 mcr.microsoft.com/mssql/server:2022-latest

#Install Angular CLI version 18 globally
npm install -g @angular/cli@18

# Create Angular 18 project
ng new campus-frontend --routing --style=scss --package-manager=npm --standalone=true

# Add Angular Material
ng add @angular/material

# Add PWA support
ng add @angular/pwa

# Add Angular CDK for advanced features
ng add @angular/cdk

#BIOME AND ESLINT
# 1. Install Biome
bun add -d @biomejs/biome

# 2. Initialize Biome (this creates biome.json automatically)
bunx @biomejs/biome init

# 3. Install ESLint for Angular-specific rules only
# Install ESLint 8 specifically
bun add -d eslint@^8.57.0


bun add -d @angular-eslint/builder @angular-eslint/eslint-plugin @angular-eslint/eslint-plugin-template @angular-eslint/schematics @angular-eslint/template-parser @typescript-eslint/eslint-plugin @typescript-eslint/parser


# 4. Install architecture enforcement plugins
bun add -d
  eslint-plugin-import
  eslint-import-resolver-typescript
  eslint-plugin-boundaries

# 5. Add Angular ESLint schematics
bunx ng add @angular-eslint/schematics

# 6.  If you have a prettier config, migrate it
bunx @biomejs/biome migrate prettier --Write

#VITEST
# Core Vitest packages for Angular
bun add -d vitest @vitest/ui @analogjs/vite-plugin-angular happy-dom

# Testing utilities
bun add -d @testing-library/angular @testing-library/jest-dom

#Husky
bunx husky init

# Git endlines to be compatible with biome
#  fully disable CRLF conversion
git config --global core.autocrlf false

# normalize line endings to LF when committing
git config --global core.eol lf

# Tailwind install dependencies
bun add -d tailwindcss postcss autoprefixer

# Initialize
bun add -d tailwindcss @tailwindcss/postcss

# SSL
openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout nginx-selfsigned.key -out nginx-selfsigned.crf

# to run nginx container
docker build -t nginx ./nginx
docker run -d -p 80:80 -p 443:443 -v "$(pwd)/dist/campus-frontend/browser:/usr/share/nginx/html:ro" --name nginx-test nginx

#let's encypt ssl
docker-compose run --rm certbot certonly --webroot -w /var/www/certbot -d campusproject08.com -d www.campusproject08.com --email asv_18_1999@hotmail.com --agree-tos --no-eff-email

#To migrate from constructor injection to the modern inject method
 ng generate @angular/core:inject --path src/app/features/auth/infrastructure
